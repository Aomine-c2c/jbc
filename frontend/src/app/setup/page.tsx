import { SetupClient } from './SetupClient';

export const metadata = {
  title: 'First-Time Server Setup | Bikita Minerals DWRMS',
  description: 'First-time authoritative server installation and configuration wizard.',
};

export default function SetupPage() {
  return <SetupClient />;
}
